# Combined scheduler

Contains the script that using the Combined algorithm, creates a JSON distribution of pods.

## Presquites:
- The initial costs JSON calculated by *balancer_scheduler/fetch_opencost_costs.py*
- The netmarks flow JSON calculated by *netmarks_scheduler/calculate_flow.py* 